server:
  port: 3377

spring:
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848   #nacos服务注册中心地址
      config:
        server-addr: localhost:8848   #nacos服务发现中心地址
        file-extension: yaml
  application:
    name: cloud-nacos-config-client


#${spring.application.name}-${spring.profile.active}. ${spring.cLoud.nacos.config.file-extension}
#cloud-nacos-config-client-dev.yml   #在nacos中配置文件名


